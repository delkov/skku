
(setq ff (open "C:\\Users\\delko_000\\Desktop\\temp.txt" "r"))
(if (= read-line ff 0); пробовал "" вместо nil
(setq test 10)
);
(if (= read-line ff 0); пробовал "" вместо nil
(setq test 10)
);
(if (= read-line ff 0); пробовал "" вместо nil
(setq test 10)
);
(if (= read-line ff 0); пробовал "" вместо nil
(setq test 10)
);
(setq test2 (read-line ff))
(setq test3 (read-line ff))
(setq test4 (read-line ff))
(setq test5 (read-line ff))
(if (= test5 ())
(setq test_epta 10)
)
(close ff)
(setq ff nil)